import { formatAddress, formatNumber } from '../data.js';
function date(value) { const d = new Date(value); return Number.isNaN(d.getTime()) ? '—' : new Intl.DateTimeFormat('en-US', { month: 'short', day: 'numeric', timeZone: 'UTC' }).format(d); }
export default function NewHoldersCard({ rows = [] }) { return <section className="flex min-h-[280px] flex-col border border-rule bg-surface p-3" aria-labelledby="new-holders-title"><header className="flex items-start justify-between pb-1.5"><div><p className="m-0 text-[12px] text-[#3c3c3c]">New holders</p></div><span className="font-mono text-[13px] text-[#999]">{rows.length} in 7d</span></header><div className="flex flex-1 flex-col justify-center divide-y divide-[#f1f1f0]">{rows.slice(0, 7).map((row) => <div className="grid grid-cols-[1fr_auto_auto] items-center gap-2 py-2 text-[13px]" key={row.owner}><span className="truncate font-mono">{formatAddress(row.owner)}</span><span className="font-mono tabular-nums">{formatNumber(row.amount)}</span><span className="font-mono text-[13px] text-muted">{date(row.firstMs)}</span></div>)}</div></section>; }
