import { formatAddress, formatNumber } from '../data.js';
function date(value) { const d = new Date(value); return Number.isNaN(d.getTime()) ? '—' : new Intl.DateTimeFormat('en-US', { month: 'short', day: 'numeric', timeZone: 'UTC' }).format(d); }
export default function NewHoldersCard({ rows = [] }) { return <section className="min-h-[280px] border border-rule bg-surface p-2" aria-labelledby="new-holders-title"><header className="flex items-start justify-between border-b border-rule pb-2"><div><p className="m-0 font-mono text-[10px] uppercase tracking-wide text-muted">Recent cohort</p><h2 id="new-holders-title" className="m-0 mt-1 text-xs font-bold uppercase">New holders</h2></div><span className="font-mono text-[10px] text-muted">{rows.length} in 7d</span></header><div className="divide-y divide-[#d2d0ca]">{rows.slice(0, 7).map((row) => <div className="grid grid-cols-[1fr_auto_auto] items-center gap-2 py-2 text-xs" key={row.owner}><span className="truncate font-mono">{formatAddress(row.owner)}</span><span className="font-mono tabular-nums">{formatNumber(row.amount)}</span><span className="font-mono text-[10px] text-muted">{date(row.firstMs)}</span></div>)}</div></section>; }
