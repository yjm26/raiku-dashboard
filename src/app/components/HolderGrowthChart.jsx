import { Area, AreaChart, CartesianGrid, ResponsiveContainer, Tooltip, XAxis, YAxis } from 'recharts';
import ChartCard from './ChartCard.jsx';
import { formatNumber } from '../data.js';
export default function HolderGrowthChart({ data = [] }) { return <ChartCard title="Holder growth" eyebrow="real wallets since launch">{data.length ? <ResponsiveContainer width="100%" height={245}><AreaChart data={data} margin={{ top: 12, right: 12, left: -14, bottom: 0 }}><CartesianGrid stroke="var(--chart-grid)" vertical={false} /><XAxis dataKey="label" tick={{ fill: 'var(--chart-tick)', fontSize: 10 }} tickLine={false} axisLine={{ stroke: 'var(--chart-axis)' }} minTickGap={24} /><YAxis tick={{ fill: 'var(--chart-tick)', fontSize: 10 }} tickLine={false} axisLine={{ stroke: 'var(--chart-axis)' }} tickFormatter={(v) => formatNumber(v, { maximumFractionDigits: 0 })} width={42} /><Tooltip contentStyle={{ background: 'var(--surface)', border: '1px solid var(--rule)', color: 'var(--ink)', fontSize: 11 }} formatter={(v) => [formatNumber(v, { maximumFractionDigits: 0 }), 'Wallets']} /><Area type="monotone" dataKey="holders" stroke="var(--chart-holders)" strokeWidth={2} fill="var(--chart-holders)" fillOpacity={0.08} /></AreaChart></ResponsiveContainer> : <div className="font-serif text-[12px] text-muted" role="status">Click &quot;Run&quot; to get results</div>}</ChartCard>; }
