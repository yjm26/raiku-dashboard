import { Area, AreaChart, CartesianGrid, ResponsiveContainer, Tooltip, XAxis, YAxis } from 'recharts';
import ChartCard from './ChartCard.jsx';
import { formatNumber } from '../data.js';
export default function HolderGrowthChart({ data = [] }) { return <ChartCard title="Holder growth" eyebrow="real wallets since launch">{data.length ? <ResponsiveContainer width="100%" height={245}><AreaChart data={data} margin={{ top: 12, right: 12, left: -14, bottom: 0 }}><CartesianGrid stroke="#f1f1f0" vertical={false} /><XAxis dataKey="label" tick={{ fill: '#999', fontSize: 10 }} tickLine={false} axisLine={{ stroke: '#e8e8e6' }} minTickGap={24} /><YAxis tick={{ fill: '#999', fontSize: 10 }} tickLine={false} axisLine={{ stroke: '#e8e8e6' }} tickFormatter={(v) => formatNumber(v, { maximumFractionDigits: 0 })} width={42} /><Tooltip contentStyle={{ background: '#fff', border: '1px solid #e8e8e6', color: '#252525', fontSize: 11 }} formatter={(v) => [formatNumber(v, { maximumFractionDigits: 0 }), 'Wallets']} /><Area type="monotone" dataKey="holders" stroke="#252525" strokeWidth={2} fill="#252525" fillOpacity={0.06} /></AreaChart></ResponsiveContainer> : <div className="font-serif text-[10px] text-[#777]" role="status">Click &quot;Run&quot; to get results</div>}</ChartCard>; }
