import { formatAddress, formatNumber } from '../data.js';
export default function TopHoldersCard({ rows = [] }) { return <section className="min-h-[280px] border border-rule bg-surface p-2" aria-labelledby="top-holders-title"><header className="flex items-start justify-between border-b border-rule pb-2"><div><p className="m-0 font-mono text-[10px] uppercase tracking-wide text-muted">Distribution</p><h2 id="top-holders-title" className="m-0 mt-1 text-xs font-bold uppercase">Top holders</h2></div><span className="font-mono text-[10px] text-muted">by balance</span></header><div className="divide-y divide-[#d2d0ca]">{rows.slice(0, 7).map((row) => <div className="grid grid-cols-[1.5rem_1fr_auto] items-center gap-2 py-2 text-xs" key={row.owner}><span className="font-mono text-muted">{row.rank}</span><span className="truncate font-mono">{formatAddress(row.owner)}</span><span className="font-mono tabular-nums">{formatNumber(row.amount)}</span></div>)}</div></section>; }
