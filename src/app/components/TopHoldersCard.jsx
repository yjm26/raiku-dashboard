import { formatAddress, formatNumber } from '../data.js';
export default function TopHoldersCard({ rows = [] }) { return <section className="flex min-h-[280px] flex-col border border-rule bg-surface p-3" aria-labelledby="top-holders-title"><header className="flex items-start justify-between pb-1.5"><div><p className="m-0 text-[10px] text-[#3c3c3c]">Top holders</p></div><span className="font-mono text-[9px] text-[#999]">by balance</span></header><div className="flex flex-1 flex-col justify-center divide-y divide-[#f1f1f0]">{rows.slice(0, 7).map((row) => <div className="grid grid-cols-[1.5rem_1fr_auto] items-center gap-2 py-2 text-[11px]" key={row.owner}><span className="font-mono text-[10px] text-muted">{row.rank}</span><span className="truncate font-mono">{formatAddress(row.owner)}</span><span className="font-mono tabular-nums">{formatNumber(row.amount)}</span></div>)}</div></section>; }
