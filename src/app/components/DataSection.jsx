import { useMemo, useState } from 'react';
import HoldersTable from './HoldersTable.jsx';
import PointsTable from './PointsTable.jsx';

export default function DataSection({ rows = [] }) { const [tab, setTab] = useState('holders'); const [filter, setFilter] = useState(''); const visible = useMemo(() => { const q = filter.trim().toLowerCase(); return q ? rows.filter((r) => r.owner.toLowerCase().includes(q)) : rows; }, [rows, filter]); return <section className="data-section" aria-labelledby="data-title"><div className="section-heading section-heading--data"><div><p className="section-heading__eyebrow">Explore the snapshot</p><h2 id="data-title">Holder data</h2></div><input aria-label="Filter holders" value={filter} onChange={(e) => setFilter(e.target.value)} placeholder="Filter address…" /></div><div className="data-tabs" role="tablist" aria-label="Holder data views"><button role="tab" aria-selected={tab === 'holders'} className={tab === 'holders' ? 'is-active' : ''} onClick={() => setTab('holders')}>All holders</button><button role="tab" aria-selected={tab === 'points'} className={tab === 'points' ? 'is-active' : ''} onClick={() => setTab('points')}>Points leaderboard</button><span className="data-count">{visible.length.toLocaleString()} rows</span></div>{tab === 'holders' ? <HoldersTable rows={visible} /> : <PointsTable rows={visible} />}</section>; }
