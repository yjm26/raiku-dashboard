import ExternalLink from './ExternalLink.jsx';
import { formatAddress, formatNumber } from '../data.js';
export default function HoldersTable({ rows = [] }) { return <div className="table-wrap"><table><caption className="sr-only">All rkuSOL holders</caption><thead><tr><th>#</th><th>Wallet</th><th>Type</th><th className="num">rkuSOL</th><th className="num">Share</th><th className="num">Days</th><th className="num">Points</th></tr></thead><tbody>{rows.map((r, i) => <tr key={r.owner}><td>{i + 1}</td><td><ExternalLink href={`https://solscan.io/account/${r.owner}`}><span className="mono">{formatAddress(r.owner)}</span></ExternalLink></td><td>{r.isPda ? <span className="table-type table-type--pool">{r.pdaLabel || 'Pool / program'}</span> : <span className="table-type">Wallet</span>}</td><td className="num">{formatNumber(r.amount)}</td><td className="num">{formatNumber(r.sharePct, { maximumFractionDigits: 2 })}%</td><td className="num">{formatNumber(r.daysHeld, { maximumFractionDigits: 1 })}</td><td className="num">{formatNumber(r.score, { maximumFractionDigits: 0 })}</td></tr>)}</tbody></table></div>; }
