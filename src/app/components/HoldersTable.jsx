import ExternalLink from './ExternalLink.jsx';
import { formatAddress, formatNumber } from '../data.js';
export default function HoldersTable({ rows = [], startRank = 0 }) { return <div className="mt-2 overflow-x-auto border border-rule bg-surface"><table className="w-full min-w-[760px] border-collapse text-[13px]"><caption className="sr-only">All rkuSOL holders</caption><thead className="bg-surface-muted text-left font-mono text-[12px] uppercase text-muted"><tr><th className="px-3 py-3">#</th><th className="px-3 py-3">Wallet</th><th className="px-3 py-3">Type</th><th className="px-3 py-3 text-right">rkuSOL</th><th className="px-3 py-3 text-right">Share</th><th className="px-3 py-3 text-right">Days</th><th className="px-3 py-3 text-right">Points</th></tr></thead><tbody>{rows.map((r, i) => <tr className="border-t border-[#d2d0ca]" key={r.owner}><td className="px-3 py-3 font-mono text-muted">{startRank + i + 1}</td><td className="px-3 py-3"><ExternalLink href={`https://solscan.io/account/${r.owner}`}><span className="font-mono text-accent underline underline-offset-4">{formatAddress(r.owner)}</span></ExternalLink></td><td className="px-3 py-3 text-muted">{r.isPda ? r.pdaLabel || 'Pool / program' : 'Wallet'}</td><td className="px-3 py-3 text-right font-mono tabular-nums">{formatNumber(r.amount)}</td><td className="px-3 py-3 text-right font-mono tabular-nums">{formatNumber(r.sharePct, { maximumFractionDigits: 2 })}%</td><td className="px-3 py-3 text-right font-mono tabular-nums">{formatNumber(r.daysHeld, { maximumFractionDigits: 1 })}</td><td className="px-3 py-3 text-right font-mono tabular-nums">{formatNumber(r.score, { maximumFractionDigits: 0 })}</td></tr>)}</tbody></table></div>; }
