import { Area, AreaChart, CartesianGrid, ResponsiveContainer, Tooltip, XAxis, YAxis } from 'recharts';
import ChartCard from './ChartCard.jsx';
import { formatNumber } from '../data.js';
export default function PointsAccrualChart({ data = [] }) { return <ChartCard title="Points accrual" eyebrow="Cumulative estimate">{data.length ? <ResponsiveContainer width="100%" height={245}><AreaChart data={data} margin={{ top: 12, right: 12, left: -16, bottom: 0 }}><CartesianGrid stroke="#d2d0ca" vertical={false} /><XAxis dataKey="label" tick={{ fill: '#66645f', fontSize: 10 }} tickLine={false} axisLine={{ stroke: '#171717' }} minTickGap={24} /><YAxis tick={{ fill: '#66645f', fontSize: 10 }} tickLine={false} axisLine={{ stroke: '#171717' }} tickFormatter={(v) => formatNumber(v, { maximumFractionDigits: 0 })} width={58} /><Tooltip contentStyle={{ background: '#fff', border: '1px solid #171717', color: '#111', borderRadius: 0 }} formatter={(v) => [formatNumber(v, { maximumFractionDigits: 0 }), 'Points']} /><Area type="monotone" dataKey="points" stroke="#3458d4" strokeWidth={3} fill="#e9edff" fillOpacity={0.7} /></AreaChart></ResponsiveContainer> : <div className="font-mono text-xs text-muted" role="status">Run analysis to populate this panel.</div>}</ChartCard>; }
