import ExternalLink from './ExternalLink.jsx';
import { formatAddress, formatNumber } from '../data.js';
export default function PointsTable({ rows = [], startRank = 0 }) { const sorted = [...rows].sort((a, b) => b.score - a.score); return <div className="mt-2 overflow-x-auto border border-rule bg-surface"><table className="w-full min-w-[680px] border-collapse text-[13px]"><caption className="sr-only">Estimated points leaderboard</caption><thead className="bg-surface-muted text-left font-mono text-[12px] uppercase text-muted"><tr><th className="px-3 py-3">#</th><th className="px-3 py-3">Wallet</th><th className="px-3 py-3 text-right">Balance</th><th className="px-3 py-3 text-right">Days held</th><th className="px-3 py-3 text-right">Estimated points</th><th className="px-3 py-3 text-right">Daily</th></tr></thead><tbody>{sorted.map((r, i) => <tr className="border-t border-rule" key={r.owner}><td className="px-3 py-3 font-mono text-muted">{startRank + i + 1}</td><td className="px-3 py-3"><ExternalLink href={`https://solscan.io/account/${r.owner}`}><span className="font-mono text-ink underline underline-offset-4">{formatAddress(r.owner)}</span></ExternalLink></td><td className="px-3 py-3 text-right font-mono tabular-nums">{formatNumber(r.amount)}</td><td className="px-3 py-3 text-right font-mono tabular-nums">{formatNumber(r.daysHeld, { maximumFractionDigits: 1 })}</td><td className="px-3 py-3 text-right font-mono tabular-nums">{formatNumber(r.score, { maximumFractionDigits: 0 })}</td><td className="px-3 py-3 text-right font-mono tabular-nums">+{formatNumber(r.amount)}/d</td></tr>)}</tbody></table></div>; }
