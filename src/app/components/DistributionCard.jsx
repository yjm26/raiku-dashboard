import { formatNumber } from '../data.js';
export default function DistributionCard({ snapshot }) { const top = Number(snapshot?.stats?.top10Share || 0); const others = Math.max(0, 100 - top); return <section className="flex min-h-[280px] flex-col border border-rule bg-surface p-3" aria-labelledby="distribution-title"><header className="flex items-start justify-between pb-1.5"><div><p className="m-0 text-[10px] text-ink">Holder distribution</p></div><span className="font-mono text-[9px] text-muted">top 10</span></header><div className="flex min-h-[245px] flex-1 flex-col justify-center"><strong className="font-mono text-4xl font-normal tracking-[-0.06em] text-ink">{formatNumber(top, { maximumFractionDigits: 1 })}%</strong><span className="mt-1 text-[10px] text-muted">of supply held by the top ten wallets</span><div className="mt-6 flex h-2.5 w-full overflow-hidden rounded-full bg-surface-muted"><span className="bg-accent" style={{ width: `${Math.min(100, top)}%` }} /><span className="bg-surface-muted" style={{ width: `${Math.min(100, others)}%` }} /></div><div className="mt-2 flex justify-between font-mono text-[9px] text-muted"><span>top 10 {formatNumber(top, { maximumFractionDigits: 1 })}%</span><span>others {formatNumber(others, { maximumFractionDigits: 1 })}%</span></div></div></section>; }
